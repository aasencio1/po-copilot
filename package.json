{
  "name": "po-copilot",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core -w packages/rules -w packages/generators -w packages/exporters",
    "lint": "npm -w apps/api run lint",
    "test": "npm -w apps/api run test"
  },
  "dependencies": {
    "@nestjs/axios": "^4.0.1",
    "axios": "^1.12.2",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2"
  }
}
